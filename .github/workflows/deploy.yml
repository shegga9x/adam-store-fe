name: 🚀 Deploy Microservice to VPS

on:
  push:
    branches:
      - update-vps # or your specific deploy branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    environment: secrets
    steps:
      - name: 📦 SSH Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            set -euo pipefail

            PROJECT_DIR="/root/f4-microserices-vps-configuration/Spring_AdamStore"
            CONTAINER_APP="spring-adam-store"
            HEALTH_URL="https://${{ secrets.VPS_HOST }}/adamstore/actuator/health"

            echo "📁 Navigating to project directory..."
            cd $PROJECT_DIR

            echo "📥 Pulling latest changes from Git..."
            git pull

            echo "🧼 Removing existing app container if any..."
            docker compose down $CONTAINER_APP

            echo "🐳 Building and starting all services..."
            docker compose up -d --build

            echo "🔎 Waiting for $CONTAINER_APP to become healthy..."
            until curl -sf $HEALTH_URL | jq -r .status | grep -q UP; do
              echo "⏳ Waiting for $CONTAINER_APP to be healthy..."
              sleep 3
            done
            sleep 6

            echo "✅ Deployment finished successfully."
