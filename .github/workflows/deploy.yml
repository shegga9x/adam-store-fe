name: Deploy Microservice to VPS

on:
  push:
    branches:
      - update-vps # Or your deploy branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          # Write the key and ensure proper line endings
          # Write the key and ensure proper | tr -d '\r' line endings
          echo "${{ secrets.VPS_SS
          # Verify key format
          if ! ssh-keygen -l -f ~/.ssh/vps_key; then
            echo "Invalid SSH key format"
            exit 1
          fiH_KEY }}" | tr -d '\r' > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key
          # Verify key format
          if ! ssh-keygen -l -f ~/.ssh/vps_key; then
            echo "Invalid SSH key format"
            exit 1
          fi
          ssh-keyscan -H microservices.appf4.io.vn >> ~/.ssh/known_hosts
        shell: bash

      - name: SSH into VPS and deploy
        run: |
          ssh -i ~/.ssh/vps_key -o StrictHostKeyChecking=no root@microservices.appf4.io.vn << 'EOF'
            echo '✅ Connected to VPS'

            # Go to project and deploy
            cd /root/your-app-folder
            git pull origin update-vps

            docker compose down
            docker compose up -d --build

            echo '🚀 Deployment complete!'
          EOF
